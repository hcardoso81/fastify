{
  "type": "module",
  "dependencies": {
    "fastify": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "nodemon": "^2.0.22",
    "typescript": "^4.5.2"
  },
  "scripts": {
    "start": "nodemon --watch src --ext ts --exec \"yarn build && node dist/server.js\"",
    "build": "tsc -p ."
  }
}
